/** Mirrors `ACE.Cloud.Backend.AuthSessionEndpoints.CsrfHeaderName` exactly. */
export const AuthSessionCsrfHeaderName = "X-Csrf-Token";

/** Mirrors `ACE.Cloud.Backend.AccountEndpoints.IdempotencyKeyHeaderName` exactly (transaction rule 4). */
export const IdempotencyKeyHeaderName = "Idempotency-Key";
