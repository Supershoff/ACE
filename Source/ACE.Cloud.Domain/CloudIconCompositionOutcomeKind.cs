namespace ACE.Cloud.Domain;

/// <summary>Whether a composition attempt produced a real icon or the neutral fallback (UI-006).</summary>
public enum CloudIconCompositionOutcomeKind
{
    Composed,
    Fallback,
}
